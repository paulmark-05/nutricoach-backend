## Packages
recharts | For visualizing macro progress charts
framer-motion | For smooth animations and page transitions
lucide-react | Beautiful icons for the UI (already in base, but emphasizing usage)
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
- Theme is Pastel: Pink (#FFB3D9), Blue (#B3D9FF), Green (#B3FFD9), Yellow (#FFE5B3), Purple (#D9B3FF).
- Font: 'Outfit' from Google Fonts.
- Using Replit Auth for authentication.
- Images are handled as base64 strings or external URLs.
